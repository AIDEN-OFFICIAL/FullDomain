// Find the object is empty or not
let ob = { hii: 0 };
let c = Object.keys(ob).length === 0 ? "ob is empty" : "ob exist";
//Object.keys is an object method that defines the keys inside the object
console.log(c);
